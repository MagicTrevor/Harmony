# https://docs.codecov.io/docs/codecov-yaml
# https://github.com/codecov/support/wiki/Codecov-Yaml

coverage:
  status:
    project:
      default: false
    patch:
      default: false

flags:
  production:
    paths:
      - src/Harmony.Core/
      - src/Harmony.Data/
      - src/Harmony.Data.EntityFrameworkCore/
      - src/Harmony.Web/
  test:
    paths:
      - test/Harmony.Tests.Data.EntityFrameworkCore/
